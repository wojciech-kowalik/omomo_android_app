<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:paddingBottom="@dimen/omomo_launcher_container_margin_bottom"
             android:paddingLeft="@dimen/omomo_launcher_container_margin_left"
             android:paddingRight="@dimen/omomo_launcher_container_margin_right"
             android:paddingTop="@dimen/omomo_launcher_container_margin_top"
             android:background="@color/omomo_navy"
             android:orientation="vertical"
             tools:context="pl.visualnet.omomo.ui.SplashActivity">

    <ImageView
            android:id="@+id/image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:layout_gravity="top|center_horizontal"
            android:src="@drawable/logo"
            android:layout_marginTop="@dimen/omomo_launcher_image_margin_top"
            android:contentDescription="@string/omomo_logo_description"/>

    <TextView
            android:id="@+id/omomo_app_version"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="left"
            android:layout_gravity="bottom|right"
            android:text="@string/omomo_version_content"
            android:textColor="@android:color/white"
            android:textSize="@dimen/omomo_launcher_version"
            android:textStyle="normal"/>

</FrameLayout>
